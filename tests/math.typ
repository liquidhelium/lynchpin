= Math Test

Inline: $a + b = c$ and $x^2 + y^2 = z^2$.

Fraction: $a/b$ and display:

$ (x + y) / (a - b) $

Square root: $sqrt(x + 1)$ and $root(3, x^2)$.

Big square root: $ sqrt(1+sqrt(1/2)) $

Integral:

$ integral_((sin t)/2 times t )^infinity e^(-x)/2/2/2/2 dif x = 1 $

Matrix:

$ mat(1, 2; 3, 4) $

Sum:

$ sum_(i=0)^n i = (n(n+1))/2 $

$ sum = n^3/2+n^2/2+n/6 $

Prod:

$ product_(i=0)^100 $

Cases:

$ cases("1", "2","1", "2","1", "2","1",#[$1/2/3/4$1234],delim: "[") $

$ forall n in NN, exists m, m = n + 1 $

$ m n p $
$ H attach(limits(stretch(=,)),t: "12345") U + p V $
$ H attach(limits(stretch(=,size:#150%)),t: "中文测试") U + p V $
$ H attach(limits(stretch(<=>,size:#150%)),t: "中文测试") U + p V $
$ H attach(limits(stretch(<->,size:#150%)),t: "中文测试") U + p V $
$ underline(1 + 2 + ... + 5) $
$ underbrace(0 + 1 + dots.c + n, n + 1 "numbers") $
$ overbrace(0 + 1 + dots.c + n, n + 1 "numbers") $
$ underbracket(0 + 1 + dots.c + n, n + 1 "numbers") $
$ overbracket(0 + 1 + dots.c + n, n + 1 "numbers") $
$ underparen(0 + 1 + dots.c + n, n + 1 "numbers") $
$ undershell(0 + 1 + dots.c + n, n + 1 "numbers") $
Total displaced soil by glacial flow:

Total displaced soil by glacial flow:

$ 7.32 beta +
  sum_(i=0)^nabla
    (Q_i (a_i - epsilon)) / 2 $

$ a >> b $
