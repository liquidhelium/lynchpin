= Math Test

Inline: $a + b = c$ and $x^2 + y^2 = z^2$.

Fraction: $a/b$ and display:

$ (x + y) / (a - b) $

Square root: $sqrt(x + 1)$ and $root(3, x^2)$.

Big square root: $ sqrt(1+sqrt(1/2)) $

Integral:

$ integral_0^infinity e^(-x) dif x = 1 $

Matrix:

$ mat(1, 2; 3, 4) $

Sum:

$ sum_(i=0)^n i = (n(n+1))/2 $
